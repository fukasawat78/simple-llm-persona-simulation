# End-user API keys are entered in the UI and must never be stored here.
OPENAI_MODEL=gpt-5.6-luna
PORT=4173
ALLOW_DEMO_MODE=true
# Use a long random value in persistent environments.
SESSION_SECRET=replace-with-at-least-32-random-bytes
