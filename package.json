{
  "name": "persona-churn-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mobile subscriber churn simulation with a human-in-the-loop workflow",
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
